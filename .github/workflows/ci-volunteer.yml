name: CI - volunteer Service (Python)

on:
  push:
    branches: [main, develop]
    paths:
      - 'volunteer-service/**'
      - '.github/workflows/ci-volunteer.yml'
      - '.github/workflows/ci-python-reusable.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'volunteer-service/**'
      - '.github/workflows/ci-volunteer.yml'
      - '.github/workflows/ci-python-reusable.yml'

permissions:
  contents: read
  security-events: write
  packages: read

jobs:
  call-python-ci:
    uses: ./.github/workflows/ci-python-reusable.yml
    with:
      service_name: volunteer-service
      service_path: volunteer-service
      python_version: '3.11'
    secrets:
      aws_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
      aws_region: ${{ secrets.AWS_REGION }}
      aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
